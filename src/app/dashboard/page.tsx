export default function DashboardPage() {
  return (
    <div className="mx-auto max-w-6xl">
      <h1 className="text-3xl font-bold tracking-tight">Dashboard</h1>
      <p className="mt-1 text-muted-foreground">Your developer knowledge hub</p>

      {/* Placeholder — collections grid and pinned items come in a later phase */}
      <h2 className="mt-8 text-sm font-medium text-muted-foreground">Main</h2>
    </div>
  );
}
